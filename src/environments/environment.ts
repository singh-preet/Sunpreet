// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig:  {
    apiKey: "AIzaSyCqwFkZuf55Ttj-rHaQjhKd8FIhqXhmYNc",
    authDomain: "gemselections-add52.firebaseapp.com",
    databaseURL: "https://gemselections-add52.firebaseio.com",
    projectId: "gemselections-add52",
    storageBucket: "gemselections-add52.appspot.com",
    messagingSenderId: "980508632732",
    appId: "1:980508632732:web:bb8de396efac5149"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
